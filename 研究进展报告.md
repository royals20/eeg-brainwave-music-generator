# EEG脑波音乐生成系统——研究进展报告

> **项目名称**：EEG Brainwave Music Generator（脑电波音乐生成器）
> **仓库地址**：`royals20/eeg-brainwave-music-generator`
> **报告日期**：2026-04-15
> **系统状态**：初步原型（Proof-of-Concept）

---

## 目录

1. [项目概述](#一项目概述)
2. [当前研究进展](#二当前研究进展)
   - 2.1 [系统流程进展](#21-系统流程进展)
   - 2.2 [核心方法进展](#22-核心方法进展)
   - 2.3 [初步验证进展](#23-初步验证进展)
3. [当前存在的问题与局限](#三当前存在的问题与局限)
4. [下一步分阶段研究计划](#四下一步分阶段研究计划)
5. [风险评估与缓解措施](#五风险评估与缓解措施)
6. [评价指标体系](#六评价指标体系)
7. [口头汇报稿（精简版）](#七口头汇报稿精简版)

---

## 一、项目概述

本项目旨在构建一套以人体脑电（EEG）信号为驱动源的音乐自动生成系统。其核心思路是：通过采集和分析大脑在特定状态（尤其是**慢波睡眠，SWS**）下产生的特征性脑电信号，利用信号处理与音乐映射算法，实时或离线地生成与脑电状态相对应的MIDI音乐和WAV音频，最终输出一段"由大脑指挥"的个性化乐曲。

系统使用 Python 语言（占比 99.1%）实现，依托 `scipy`、`mne`、`pretty_midi`、`midiutil`、`PyQt5` 等成熟开源库构建完整处理链路，并配备图形用户界面（GUI）与结果导出功能。

---

## 二、当前研究进展

### 2.1 系统流程进展

目前，系统已完整实现从"脑电数据输入"到"音频输出"的**端到端原型闭环**，共分为以下七个功能模块，均已通过代码实现并可运行：

```
EEG数据输入
    ↓
信号预处理
    ↓
特征提取
    ↓
慢波睡眠（SWS）检测
    ↓
音乐特征映射
    ↓
MIDI生成
    ↓
音频合成与播放
```

各模块实现情况如下表所示：

| 模块编号 | 模块名称 | 实现文件 | 完成状态 |
|:---:|:---|:---|:---:|
| M1 | EEG数据输入 | `eeg_processing/eeg_loader.py` | ✅ 完成 |
| M2 | 信号预处理 | `eeg_processing/preprocess.py` | ✅ 完成 |
| M3 | 频带特征提取 | `eeg_processing/feature_extractor.py` | ✅ 完成 |
| M4 | SWS检测 | `eeg_processing/sws_detector.py` | ✅ 完成 |
| M5 | 音乐特征映射 | `music/mapper.py` | ✅ 完成 |
| M6 | MIDI生成 | `music/midi_generator.py` | ✅ 完成 |
| M7 | 音频合成与播放 | `music/audio_synth.py` | ✅ 完成 |
| M8 | 图形用户界面 | `gui/` | ✅ 完成 |
| M9 | 数据库管理 | `database/db_manager.py` | ✅ 完成 |
| M10 | 结果导出（CSV） | `reports/csv_exporter.py` | ✅ 完成 |

**关键结论**：原型系统可重复运行，链路贯通，可生成可听的音乐输出。

---

### 2.2 核心方法进展

#### 2.2.1 EEG数据输入模块（M1）

`EEGLoader` 类支持三种数据来源：

- **真实数据**：通过 `mne` 库读取 EDF（欧洲数据格式）和 BDF 格式文件，支持多通道采集（默认通道：Fz、Cz、Pz、Oz）。
- **CSV文件**：可加载自定义采样率的CSV格式脑电数据，默认采样率 256 Hz。
- **仿真数据**：内置仿真生成器可生成带有伪SWS片段的多通道EEG信号（4通道，10分钟，采样率256 Hz，SWS占比约30%），用于开发和测试阶段。

#### 2.2.2 信号预处理模块（M2）

`Preprocessor` 类采用流水线（pipeline）设计，按顺序执行以下处理步骤：

1. **带通滤波**（Bandpass Filtering）：采用4阶 Butterworth 滤波器，通过 `scipy.signal.filtfilt` 进行零相位双向滤波，有效保留0.5–30 Hz范围内的脑电有效频段，同时抑制低频基线漂移和高频肌电干扰。

2. **工频陷波滤波**（Notch Filtering）：采用 `iirnotch` 设计陷波滤波器（Q因子=30），滤除50/60 Hz工频噪声。

3. **幅度归一化**（Normalization）：支持三种方法——z-score（均值为0、方差为1）、min-max（归一化至[0,1]区间）、amplitude（最大绝对值归一化），默认采用z-score。

4. **阈值伪迹处理**（Artifact Removal）：基于z-score阈值（默认3.0倍标准差）检测异常采样点，采用局部中位数插值（±10个采样点邻域）进行替换修复。

5. **信号降采样**（Downsampling）：支持按整数比例降采样，使用 `scipy.signal.resample` 保证频谱完整性。

#### 2.2.3 特征提取模块（M3）

`FeatureExtractor` 类以30秒为一个分析窗口（epoch），对每个窗口提取以下特征：

**（A）频带功率特征（Spectral Band Power）**

采用 Welch 功率谱估计（`scipy.signal.welch`，nperseg=1024），分别计算四个标准频带的积分功率：

| 频带名称 | 频率范围 | 脑电生理意义 |
|:---:|:---:|:---|
| Delta（δ） | 0.5–4.0 Hz | 深度睡眠、慢波活动的核心频段 |
| Theta（θ） | 4.0–8.0 Hz | 浅睡眠、记忆加工、困倦状态 |
| Alpha（α） | 8.0–13.0 Hz | 放松、闭目养神 |
| Beta（β） | 13.0–30.0 Hz | 警觉、专注、认知活动 |

计算公式为：

$$P_{band} = \int_{f_{low}}^{f_{high}} \text{PSD}(f) \, df \approx \sum_{f \in band} \text{PSD}(f) \cdot \Delta f$$

**（B）瞬时幅度特征（Instantaneous Amplitude）**

对0.5–2.0 Hz窄带滤波后的信号，采用 Hilbert 解析变换计算瞬时包络幅度，输出平均瞬时幅度（`avg_instantaneous_amplitude`），用于判断慢波活动强度。

**（C）慢波比例特征（Slow Wave Ratio）**

以75 μV为幅度判决阈值，统计Hilbert包络幅度超过阈值的采样点比例，作为该窗口内慢波活动的时间占比估计：

$$\text{SWR} = \frac{\#\{t \mid A(t) \geq 75\,\mu\text{V}\}}{N_{total}}$$

**（D）时域特征**

- 平均绝对幅度（`mean_amplitude`）
- 均方根幅度（`rms_amplitude`）
- 主频率（`dominant_frequency`）：Delta频段内PSD最大值对应的频率

**（E）特征平滑**

采用滑动平均窗口（默认窗口宽度=5个epoch）对连续窗口的数值特征进行时域平滑，降低相邻窗口间特征抖动对音乐输出的影响。

#### 2.2.4 慢波睡眠检测模块（M4）

`SWSDetector` 类实现了参考AASM（美国睡眠医学会）睡眠分期准则设计的SWS判决逻辑：

- 分析单元：30秒窗口
- 判决条件（双条件AND）：
  - **条件一**：该窗口的Delta频段功率 > `delta_power_threshold`（默认1×10⁻⁶）
  - **条件二**：慢波时间比例 ≥ `slow_wave_ratio_threshold`（默认0.2，即20%）

**输出统计量**：

- 总SWS持续时长（秒）
- SWS阶段平均Delta功率
- 慢波密度（个/分钟）
- SWS窗口数 / 总窗口数（SWS比例）

`IndividualizedMusicMapper.configure_from_eeg_stats()` 方法进一步对被试整段EEG计算三类水平（low/medium/high）的统计描述，用于后续个性化音乐参数配置。

#### 2.2.5 音乐特征映射模块（M5）

`IndividualizedMusicMapper` 类负责将EEG特征转换为可演奏的音乐控制参数，当前已实现以下映射规则：

**全局参数映射**（基于整段EEG的统计特征，一次性配置）：

| EEG统计特征 | 音乐参数 | 映射规则 |
|:---|:---:|:---|
| Delta功率水平（低/中/高） | 音阶选择 | 低→五声音阶；中→大调音阶；高→小调音阶 |
| 平均主频率（Hz） | 节拍速度（BPM） | <0.8 Hz → 50 BPM；0.8–1.2 Hz → 60 BPM；>1.2 Hz → 70 BPM |
| 幅度水平（低/中/高） | 乐器选择 | 低→Ambient Pad（程序号88）；中→Soft Piano（程序号0）；高→Warm Strings（程序号49） |
| 慢波密度（个/分钟） | 音符密度 | <3 → 2.0倍；3–6 → 1.0倍；>6 → 0.5倍 |

**逐窗口参数映射**（基于每个30秒窗口的特征，动态变化）：

| EEG窗口特征 | 音乐事件参数 | 映射规则 |
|:---|:---:|:---|
| 主频率（dominant_frequency） | 音高（Pitch） | 基准音高 = 60 + int(freq/4.0 × 12)，量化到当前音阶 |
| 平均幅度（mean_amplitude） | 力度（Velocity） | 线性归一化至[40, 80]区间 |
| Delta功率 > 均值 | 和声 | 在主音上方+4个半音生成和声伴音，力度降低15 |
| Delta功率 > 均值×1.2 | 扩展和声（五度音） | 在主音上方+7个半音生成第二和声 |

**音乐结构生成**：

`generate_ambient_structure()` 方法按小节（bar）为单位构建音乐框架，以4拍小节为基本单元，在Delta功率超过均值80%时加入三度音，超过均值120%时进一步叠加五度音，形成简单的和弦渐进结构，增强音乐层次感。

**MIDI输出**：支持双轨道设计（主旋律轨道 + 环境衬底轨道，程序号88），在内容不足30分钟时自动循环填充至目标时长。

#### 2.2.6 后端支撑模块（M8–M10）

- **GUI（PyQt5）**：提供被试信息录入面板（`subject_panel.py`）、EEG数据展示面板（`eeg_panel.py`）、SWS检测结果面板（`sws_panel.py`）和音乐生成控制面板（`music_panel.py`），用户可在图形界面下完成全流程操作。
- **数据库（SQLite）**：存储被试基本信息（ID、姓名、年龄、性别、PSQI评分、分组类型）及每次实验的处理记录。
- **CSV导出**：支持将SWS检测结果与音乐参数以结构化表格导出，字段包含：SWS总时长、平均Delta功率、SWS片段数、平均音高、平均速度、音乐时长等，便于后续统计分析。

---

### 2.3 初步验证进展

由于系统当前处于原型阶段，验证工作主要基于**仿真数据**和**离线功能测试**进行：

**（1）功能可达性验证（Functional Reachability）**

- 利用内置 `load_simulated()` 生成10分钟、256 Hz、含30%比例SWS片段的仿真EEG，全流程可无误运行，成功生成MIDI文件和WAV音频。
- 在不同仿真参数（SWS比例10%–50%）下，系统均能正常检测并生成差异化的音乐输出，初步证明**映射链路逻辑正确**。

**（2）音乐参数合理性验证**

- 当仿真EEG中SWS成分增多时，Delta功率水平相应升高，系统正确将音阶从大调切换为小调，节奏趋于平缓（50–60 BPM），主观听感呈现出更深沉、舒缓的风格。
- 当仿真EEG中快波（背景噪声）成分占优时，音阶保持五声音阶或大调，节奏相对较快（60–70 BPM）。
- 上述结果表明**音乐参数随脑电特征的变化趋势符合预期设计**。

**（3）工程稳定性测试**

- 预处理模块对边界条件（窗口过短、全零信号等）有保护性处理（nperseg自适应、epsilon防除零）。
- MIDI生成器在 `pretty_midi` 和 `midiutil` 两种后端均可工作，具备依赖降级容错能力。

**当前验证局限**：全部验证均基于仿真数据，尚未采集真实被试脑电数据；缺乏量化指标对音乐生成质量进行客观评估；尚未进行用户主观评价实验。

---

## 三、当前存在的问题与局限

以下问题基于对现有代码的系统性分析，按影响程度排序，并注明问题根因与对研究结论的潜在影响。

### 问题1：伪迹处理方法过于简单，信号清洁度有限

**现象**：当前伪迹去除仅采用基于z-score的阈值检测（阈值=3.0σ），使用局部中位数替换异常点。

**根本原因**：实现简单快速，但无法区分神经性信号与非神经性干扰（如眼电EOG、肌电EMG、运动伪迹），仅对极端幅度异常有响应，对连续性的低幅度伪迹无效。

**对研究的影响**：
- Delta频段（0.5–4 Hz）与眼电（0.5–5 Hz）频率高度重叠，残留眼电伪迹会虚假地抬高Delta功率，导致SWS误判率升高。
- 在真实采集场景下，信号污染程度远高于仿真数据，若不改进预处理，后续特征可信度将大幅下降。
- **影响等级：高（直接影响核心SWS检测结果的可靠性）**

---

### 问题2：仅使用单通道EEG，多通道信息未利用

**现象**：特征提取和SWS检测当前仅针对单个通道（`channel_idx=0`，对应Fz通道）进行分析，其余通道数据被加载但未参与计算。

**根本原因**：原型阶段为简化流程优先实现端到端链路，未实现多通道特征融合。

**对研究的影响**：
- 单通道EEG对SWS的空间特异性捕获能力弱。SWS慢波活动通常在前额-中央区（Fz、Cz）表现显著，而仅用Fz一个通道的信号容易受到局部伪迹干扰，鲁棒性差。
- 无法利用通道间相关性和空间滤波（如共空间模式CSP、独立成分分析ICA）提升信号质量。
- **影响等级：中（影响系统在真实数据上的鲁棒性）**

---

### 问题3：关键阈值参数为固定值，个体适应性不足

**现象**：SWS判决中的幅度阈值（75 μV）和Delta功率阈值（1×10⁻⁶）均为硬编码的固定值，未基于被试自身基线进行自适应调整。

**根本原因**：固定阈值在仿真验证阶段较为便利，但EEG幅度在不同采集设备、被试年龄、颅骨厚度等因素下差异可达数倍。

**对研究的影响**：
- 同一套阈值参数在不同被试间会产生系统性偏差：对低幅度被试（如老年人），固定75 μV阈值可能导致SWS大量漏检；对高幅度被试则可能误报。
- 音乐个性化程度不足，不同被试生成的音乐可能缺乏针对性差异。
- **影响等级：高（制约系统在真实被试实验中的有效性）**

---

### 问题4：音乐映射维度单一，主要依赖Delta频段

**现象**：当前音乐映射（mapper.py）主要依赖Delta功率、主频率、信号幅度三个特征，未利用Theta（θ）、Alpha（α）、Beta（β）频段信息。

**根本原因**：系统最初定位为SWS驱动的睡眠音乐生成器，Delta频段是SWS的核心特征，其他频段被暂时搁置。

**对研究的影响**：
- 应用场景受限：系统无法对清醒状态下的放松（Alpha主导）、专注（Beta主导）等脑状态生成有针对性的音乐。
- 节奏粒度粗糙：全局Tempo仅有三个离散档位（50/60/70 BPM），缺乏连续变化能力，音乐动态性不足。
- 和声结构简单：当前仅基于Delta功率一个条件生成根音、三度音、五度音的简单叠加，缺乏和声进行逻辑（如Ⅳ→Ⅴ→Ⅰ等功能和声）。
- **影响等级：中（影响音乐表现力，但不影响核心技术路线的正确性）**

---

### 问题5：无实时处理能力，仅支持离线批处理

**现象**：当前所有处理（预处理、特征提取、SWS检测、音乐生成）均以一次性批处理方式完成，需要先完整加载全段EEG数据再开始计算。

**根本原因**：原型设计优先保证功能正确性，未实现流式处理框架。

**对研究的影响**：
- 无法用于在线BCI（脑机接口）场景，限制了系统在实时脑状态监测和即时音乐反馈等应用方向的探索。
- 脑电采集结束后才能生成音乐，"边睡边听"的实时音乐干预范式暂时无法实现。
- **影响等级：中（影响应用场景扩展性）**

---

### 问题6：MIDI循环扩展方式机械，音乐连贯性差

**现象**：当特征段生成的音乐时长不足30分钟时，`_extend_midi()` 方法直接将已有音符块重复拼接，没有任何过渡或变奏处理。

**根本原因**：时长补全逻辑以功能优先，未加入音乐创作层面的设计。

**对研究的影响**：
- 输出音乐在循环拼接处有明显的"接缝感"，听感不自然。
- 对于评估音乐整体质量的主观实验（如疗愈效果评估）会产生负面影响，用户容易感知到重复。
- **影响等级：低（不影响技术验证，但影响用户体验评估）**

---

### 问题7：缺乏量化评价体系，结论主观性强

**现象**：代码中目前没有自动化的量化评价模块，系统效果判断主要依赖主观听感。

**根本原因**：评价体系的设计需要结合实验方案（真实被试、受控实验），在原型阶段尚未确定。

**对研究的影响**：
- 无法提供可重复、可比较的量化证据支撑研究结论。
- 缺乏客观指标无法判断不同映射策略、预处理方案之间的优劣。
- **影响等级：高（制约研究成果的学术可信度）**

---

## 四、下一步分阶段研究计划

### 第一阶段：系统稳健化与真实数据验证（预计1–2个月）

**目标**：让原型在真实EEG数据上能够稳定运行，消除高优先级问题。

**具体任务**：

1. **升级预处理管线**
   - 引入 ICA（独立成分分析）模块（基于 `mne.preprocessing.ICA`），自动识别并去除眼电（EOG）和肌电（EMG）成分。
   - 增加梯度伪迹检测（基于相邻采样点斜率阈值），对信号质量进行窗口级评分。
   - **预期产出**：信号质量评分模块，Delta功率估计误差降低30%以上。

2. **实现被试自适应阈值校准**
   - 在正式数据采集前增加1–2分钟"清醒安静基线"采集段，以被试自身Delta功率分布的百分位数（如第75百分位）动态设定SWS检测阈值。
   - 对幅度阈值（75 μV）做类似的被试自适应调整（以被试基线信号RMS的倍数关系确定）。
   - **预期产出**：个体化校准配置函数，SWS检测跨被试一致性提升。

3. **扩展多通道特征融合**
   - 在Fz、Cz、Pz三个额-顶通道上分别提取Delta功率，通过加权平均（权重参考SWS文献的通道重要性排序：Fz>Cz>Pz）生成多通道融合的Delta功率估计。
   - **预期产出**：多通道融合特征管线，抗单通道伪迹干扰能力增强。

4. **开展真实被试初步采集实验**
   - 招募3–5名被试，采集包含自然入睡过程的夜间多通道EEG数据（参考标准10–20系统，4通道）。
   - 与PSG（多导睡眠图）人工分期结果对比，评估SWS检测准确率。
   - **预期产出**：初步SWS检测准确率数据（目标：与PSG金标准的epoch级一致率≥75%）。

---

### 第二阶段：音乐方法增强与评价体系建立（预计2–3个月）

**目标**：丰富音乐映射能力，建立可量化的评价框架，填补当前方法短板。

**具体任务**：

1. **扩展多频段音乐映射**
   - 将Alpha（8–13 Hz）和Beta（13–30 Hz）频段纳入映射体系，设计"清醒音乐场景"（区别于睡眠音乐场景）：
     - Alpha功率升高 → Tempo降低、音色转向清澈弦乐或长笛、和声趋于稳定
     - Beta/Alpha比值升高（警觉指数）→ Tempo加快、节奏型音符密度增加、音高波动加大
   - 实现连续Tempo映射（替代三档离散值），支持50–100 BPM的线性/非线性插值。
   - **预期产出**：多场景音乐映射配置表（睡眠场景 + 放松场景 + 专注场景）。

2. **改进MIDI音乐结构**
   - 引入简单的和声进行规则（如Ⅰ→Ⅳ→Ⅴ→Ⅰ基础进行），在相邻窗口间进行平滑过渡（crossfade）。
   - 增加力度渐变（crescendo/diminuendo）控制，使音乐强弱变化与SWS周期节律相对应。
   - 在重复段引入轻微变奏（音高微调±1–2个半音），消除机械重复感。
   - **预期产出**：结构化音乐生成模块，循环接缝评分提升。

3. **建立量化评价体系**

   **客观指标**（可自动计算）：
   - **特征-参数映射一致性**：Spearman相关系数（Delta功率 ↔ Velocity，主频率 ↔ Pitch）
   - **参数稳定性**：连续窗口间Tempo、Pitch的标准差（越小越稳定）
   - **SWS检测性能**：以PSG为金标准，计算Sensitivity（灵敏度）、Specificity（特异度）、Cohen's κ系数
   - **音乐复杂度**：MIDI信息熵（基于音符pitch/velocity的香农熵）

   **主观指标**（需用户实验）：
   - 情绪一致性评分（1–7分李克特量表）：音乐风格与脑状态的主观匹配程度
   - 自然度评分：音乐听感是否流畅、有意义
   - 唤醒度/效价（Russell情绪环形模型）二维评分

   **预期产出**：评价指标计算模块（Python），实验记录表（CSV模板）。

---

### 第三阶段：实时化与系统集成（预计3–4个月）

**目标**：实现低延迟实时处理，将系统发展为完整的研究平台，支撑正式用户实验。

**具体任务**：

1. **实现流式处理架构**
   - 采用滑动窗口（30秒窗口、15秒步进）替代整段批处理，实现准实时特征更新。
   - 使用生产者-消费者模式（多线程队列）分离数据采集线程和音乐生成线程，目标端到端延迟<1分钟。
   - **预期产出**：实时处理模块，延迟指标测试报告。

2. **集成在线EEG采集**
   - 接入低成本EEG设备（如OpenBCI、Muse或Unicorn Hybrid Black），实现边采集边生成的真正实时闭环。
   - 在GUI中增加实时脑电波形显示和SWS状态实时提示。
   - **预期产出**：硬件接口适配模块，实时可视化界面。

3. **开展受控用户实验**
   - 实验设计：被试（n≥10）在三种条件下入睡：① 系统生成音乐；② 固定背景白噪声；③ 无声对照
   - 采集指标：SWS时长比例（客观）、主观睡眠质量评分（PSQI/VAS）、情绪评分
   - 统计分析：重复测量方差分析（Repeated Measures ANOVA）
   - **预期产出**：可支撑论文写作的受控实验数据集，初步有效性证据。

---

## 五、风险评估与缓解措施

| 风险描述 | 发生概率 | 影响程度 | 缓解措施 |
|:---|:---:|:---:|:---|
| 真实EEG数据质量差，ICA无法有效去伪迹 | 中 | 高 | 增加信号质量筛查，提高采集规范（固定被试姿势、控制环境噪声）；对质量不达标的epoch标记并剔除 |
| 被试SWS时间过短（如失眠被试），SWS检测样本不足 | 中 | 高 | 扩大被试样本量；纳入健康睡眠质量筛查（PSQI<5）；考虑纳入夜间长时记录 |
| 固定阈值校准方案不同被试间差异仍大 | 中 | 中 | 引入数据驱动阈值（GMM聚类自动确定SWS vs 非SWS的决策边界） |
| 音乐主观评分高度依赖被试音乐偏好 | 高 | 中 | 前置偏好问卷筛查；区分"音乐本身质量"和"脑状态匹配程度"两个维度的主观评分 |
| 实时处理延迟过高影响闭环体验 | 低 | 中 | 优先采用NumPy向量化运算替代循环；缓存上一窗口的特征，只在特征变化超过阈值时更新音乐参数 |
| 依赖库兼容性问题（mne/pretty_midi版本冲突） | 低 | 低 | 锁定 `requirements.txt` 版本号；编写兼容性测试；保留midiutil作为fallback后端 |

---

## 六、评价指标体系

### 6.1 技术层面指标

| 指标名称 | 计算方法 | 参考目标值 |
|:---|:---|:---:|
| SWS检测灵敏度（Sensitivity） | TP / (TP + FN)，以PSG为金标准 | ≥ 0.80 |
| SWS检测特异度（Specificity） | TN / (TN + FP） | ≥ 0.85 |
| 分期一致性（Cohen's κ） | 与PSG逐epoch对比 | ≥ 0.60（中等一致） |
| 特征平稳性（CV） | 连续窗口间特征的变异系数 | < 0.20 |
| 系统端到端延迟 | 最后一个EEG采样点 → 音符输出时间 | < 60 秒（近实时） |

### 6.2 音乐层面指标

| 指标名称 | 计算方法 | 说明 |
|:---|:---|:---|
| 映射一致性（Spearman ρ） | Delta功率 ↔ Velocity；主频率 ↔ Pitch | 验证特征到参数的单调映射关系 |
| 音乐复杂度熵值 | 音符Pitch分布的香农熵 | 反映音乐丰富程度 |
| 音乐自然度评分 | 主观7分李克特量表 | 音乐听起来是否流畅有意义 |
| 情绪一致性评分 | 主观7分李克特量表 | 音乐风格与脑状态是否匹配 |

### 6.3 系统层面指标

| 指标名称 | 评估方法 |
|:---|:---|
| 流程可复现性 | 相同输入EEG、相同随机种子，输出MIDI的note-by-note一致性 |
| 跨被试泛化性 | 在留一法（LOSO）评估下，SWS检测性能的跨被试标准差 |
| 被试主观睡眠质量 | PSQI量表前后测差值（干预组 vs 对照组） |

---

## 七、口头汇报稿（精简版）

> 以下为适用于组会/开题/中期检查等场合的**5–8分钟口头汇报稿**，可直接参照朗读。

---

**【开场定位——30秒】**

我们这个项目的核心问题是：能不能把人类大脑在睡眠过程中产生的特征性电信号，自动转换成一段与大脑状态相对应的音乐？目前，我们已经完成了一个初步的原型系统，打通了从脑电信号到音乐输出的完整技术链路，验证了方法可行性，但系统还处于早期阶段，有比较多需要深化的工作。

---

**【当前进展——2分钟】**

首先汇报进展。

**在系统层面**，我们已经实现了一个完整的端到端原型，分为七个功能模块：数据输入、信号预处理、频带特征提取、慢波睡眠检测、音乐映射、MIDI生成和音频输出。所有模块已完成编码，可以串联运行，重复产生结果。

**在方法层面**，预处理部分采用了Butterworth带通滤波和IIR陷波滤波去除工频干扰，z-score归一化，以及阈值伪迹处理。特征提取使用Welch功率谱估计计算Delta、Theta、Alpha、Beta四个频带的功率，以及基于Hilbert变换的瞬时幅度，重点提取慢波睡眠相关的Delta频段特征。SWS检测参考了AASM的判决逻辑，以30秒为窗口，用Delta功率加慢波时间比例双条件判定SWS阶段。音乐映射上，我们建立了从Delta功率等级到音阶选择、主频率到节奏速度、信号幅度到力度的映射规则，同时实现了基于Delta功率变化的和声叠加。

**在验证层面**，目前主要基于仿真数据进行了功能性测试。在不同SWS比例的仿真EEG下，系统能够正确输出差异化的音乐结果，主观听感上也可以感知到SWS成分多的EEG对应更平缓、更低沉的音乐风格，初步证明了映射逻辑的合理性。

---

**【存在问题——1.5分钟】**

当然，系统目前存在几个比较关键的问题需要正视。

**第一个，也是最重要的，是信号质量问题。** 当前伪迹处理方式非常简单，只是阈值截断，没有ICA，而Delta频段和眼电频率高度重叠，在真实数据上这会导致SWS误检率偏高，这是首要解决的问题。

**第二个是关键阈值的个体适应性不足。** 我们现在把幅度阈值固定在75微伏，这在不同被试间差异很大，需要做个体校准。

**第三个是音乐映射维度还比较单一。** 目前主要用Delta频段驱动音乐，Alpha和Beta等清醒状态的频段暂时没有利用，限制了应用场景。

**第四个是缺乏量化评价。** 目前对音乐效果的判断主要靠主观听感，没有统一的量化指标，这对后续论文写作是个明显短板。

---

**【下一步计划——2分钟】**

我们把下一步工作分三个阶段推进。

**第一阶段，重点做稳**，大约一到两个月。核心任务是升级预处理管线，引入ICA去眼电；实现被试自适应阈值校准，让系统能根据每个被试的基线自动调整参数；同时招募3到5名被试，采集真实睡眠EEG，和PSG金标准对比，初步验证SWS检测的准确性，目标一致率达到75%以上。

**第二阶段，重点做强**，大约两到三个月。扩展Alpha和Beta频段的音乐映射，覆盖更多脑状态场景；建立客观加主观的量化评价体系，包括SWS检测的Cohen's κ系数、特征到参数的Spearman相关系数，以及用户主观情绪一致性评分。

**第三阶段，重点做系统**，大约三到四个月。实现流式处理，支持准实时音乐生成；接入低成本EEG硬件，实现真正的在线脑电-音乐闭环；并开展n≥10名被试的受控实验，以SWS时长比例和主观睡眠质量作为主要结果指标，进行组间统计检验。

---

**【总结——30秒】**

总结一下：我们的项目目前已经验证了"EEG信号可以驱动音乐生成"这一核心技术路线，原型闭环可以运行。主要瓶颈在信号质量、个体适应性和评价体系三个方面，下一步将沿着"稳定性→多样性→实时化"三条主线分阶段推进，目标是将原型逐步发展为可支撑正式实验的研究平台。

---

*报告结束。如需进一步了解具体技术实现细节，请参阅仓库各模块源代码及注释。*
