# EEG Brainwave Music Generator — 研究进展汇报 PPT 方案

> **适用场景**：课题组组会、开题/中期检查、学术研讨汇报  
> **项目定位**：初步原型（Proof of Concept），已打通技术闭环，尚处于早期研究阶段  
> **仓库地址**：`royals20/eeg-brainwave-music-generator`

---

## 整体框架

```
封面 → 研究背景 → 系统架构总览 → 核心技术原理（4页）→ 当前研究进展
     → 存在的问题 → 下一步计划 → 总结与讨论
```

共 **13 页**，汇报时长约 **15–20 分钟**。

---

## 逐页内容

### 第 1 页：封面

**标题**：EEG 脑波音乐生成系统  
**副标题**：基于睡眠脑电慢波检测的个体化音乐映射原型研究  
**信息栏**：姓名 / 汇报日期 / GitHub：`royals20/eeg-brainwave-music-generator`

**口述要点（20 秒）**
> 本研究聚焦于如何利用睡眠脑电（EEG）中的慢波信号，自动生成与个体生理状态相适应的音乐。目前已完成可运行的原型系统，今天主要汇报进展、存在的问题以及下一步计划。

---

### 第 2 页：研究背景与意义

**PPT 要点**

- 脑电（EEG）可反映大脑实时状态，信息量丰富但处理难度大
- 慢波睡眠（SWS）是睡眠质量的关键指标，与记忆巩固、身体恢复密切相关
- 现有音乐生成方法多基于规则或文本，**鲜有直接利用生理信号**驱动的系统
- **核心问题**：如何将 EEG 慢波特征实时映射为有意义、个体化的音乐输出？

**应用价值**
- 睡眠辅助与疗愈音乐自动生成
- 脑机接口（BCI）领域的创意应用探索
- 个体化神经反馈（Neurofeedback）系统原型

**口述要点（40 秒）**
> 我们希望利用脑电信号中最具代表性的特征——慢波，来驱动音乐的生成。不是随机作曲，而是让音乐真正镜像大脑的生理状态变化，这对睡眠监测与音乐疗愈均有潜在价值。

---

### 第 3 页：总体系统架构

**建议放一张完整的流程图，可参考以下文字结构：**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     EEG 脑波音乐生成系统 — 处理流程                      │
├──────────┬──────────┬──────────┬──────────┬──────────┬──────────────────┤
│  数据输入 │  预处理  │ SWS检测  │ 特征提取 │ 音乐映射 │     音乐输出     │
│          │          │          │          │          │                  │
│ EDF/BDF  │ 带通滤波 │ 增量窗口 │ 频带功率 │ 个体化   │ MIDI 生成        │
│ CSV      │ 陷波滤波 │ 慢波检测 │ 主频率   │ 参数配置 │ 音频合成         │
│ 仿真信号 │ 去伪迹   │ AASM分类 │ 瞬时振幅 │ 音符映射 │ 文件导出         │
└──────────┴──────────┴──────────┴──────────┴──────────┴──────────────────┘
```

**各模块一句话说明**

| 模块 | 实现文件 | 核心功能 |
|------|---------|---------|
| 数据输入 | `eeg_processing/eeg_loader.py` | 支持 EDF/BDF/CSV 及内置仿真信号 |
| 预处理 | `eeg_processing/preprocess.py` | 滤波、去伪迹、归一化 |
| SWS 检测 | `eeg_processing/sws_detector.py` | 慢波睡眠自动识别 |
| 特征提取 | `eeg_processing/feature_extractor.py` | 频带功率、振幅、主频率 |
| 音乐映射 | `music/mapper.py` | 个体化 EEG→音乐参数转换 |
| 音乐输出 | `music/midi_generator.py` | MIDI 生成与音频合成 |

**口述要点（60 秒）**
> 整个系统分六个环节。数据可以来自真实 EDF 文件或内置仿真信号；预处理负责把信号清洗干净；SWS 检测模块识别慢波睡眠片段；特征提取把每个 30 秒的窗口转化为可量化的数值；映射模块根据个体统计特征配置音乐参数；最终输出 MIDI 或音频文件。重点在中间三个模块，下面逐一介绍原理。

---

### 第 4 页：核心技术原理（一）— EEG 信号基础

**PPT 要点**

**什么是 EEG？**
- 通过头皮电极记录神经元集群的突触后电位，信号幅度约 10–100 μV
- 本质是时间序列 $x(t)$，采样率通常为 256 Hz

**频带划分（可做表格）**

| 频带 | 范围 | 对应状态 | 本系统是否使用 |
|------|------|---------|--------------|
| Delta (δ) | 0.5–4 Hz | 深度睡眠 / 慢波睡眠 | ✅ 核心特征 |
| Theta (θ) | 4–8 Hz | 困倦 / 记忆加工 | ✅ 辅助特征 |
| Alpha (α) | 8–13 Hz | 闭眼放松 | ✅ 辅助特征 |
| Beta (β) | 13–30 Hz | 警觉 / 认知负荷 | ✅ 辅助特征 |

**为什么关注 Delta 波？**
- 高振幅（≥75 μV）、低频（0.5–2 Hz）的 Delta 波是慢波睡眠的核心标志
- AASM 标准：Delta 功率超过最小功率阈值（默认 $10^{-6}$，可调）+ 慢波时间比例 ≥ 20% → 判定为 SWS

**口述要点（60 秒）**
> EEG 信号按频率可以拆分成不同频带，每个频带和特定的大脑状态对应。本系统特别关注 Delta 波——高振幅、低频的慢波，它是深度睡眠的核心标志。我们要做的事情，就是先把这些慢波识别出来，再用它们的特征来控制音乐。

---

### 第 5 页：核心技术原理（二）— 预处理流程

**PPT 要点**

**为什么要预处理？**
- 原始 EEG 包含多种噪声：电源干扰（50 Hz）、眼电伪迹、肌电伪迹、基线漂移
- 不清洗信号 → 特征失真 → 音乐受"噪声驱动"而非"脑状态驱动"

**预处理步骤（按顺序）**

```
原始 EEG
  │
  ▼  带通滤波（0.5–30 Hz，4阶 Butterworth）
     └─ 保留有效脑电频段，抑制基线漂移与高频噪声
  │
  ▼  陷波滤波（50/60 Hz，品质因子 Q=30）
     └─ 消除工频干扰
  │
  ▼  伪迹去除（Z-score 阈值法，默认阈值 3σ）
     └─ 异常采样点替换为局部中位数
  │
  ▼  归一化（Z-score / MinMax / 振幅归一化）
     └─ 统一量纲，便于跨被试比较
  │
  ▼  清洁 EEG 信号
```

**关键代码参数**（来自 `preprocess.py`）
- 带通范围：`low=0.5 Hz, high=30 Hz`
- 滤波器阶数：`order=4`
- 伪迹阈值：`threshold=3.0`（标准差的倍数）

**口述要点（60 秒）**
> 预处理是保证后续结果可信的前提。我们依次做了带通滤波去除频段外噪声、陷波滤波去除电源干扰、Z-score 伪迹去除处理异常峰值，最后归一化。如果跳过这一步，提取出来的特征会被噪声污染，生成的音乐就不是脑状态驱动的，而是噪声驱动的。

---

### 第 6 页：核心技术原理（三）— 慢波检测（SWS Detection）

**PPT 要点**

**Step 1：Delta 功率计算（Welch 法）**

$$P_{\delta} = \int_{0.5}^{4} \text{PSD}(f) \, df \approx \sum_{f \in [0.5, 4]} \text{PSD}(f) \cdot \Delta f$$

- 使用 Welch 功率谱密度估计（`nperseg = min(1024, N)`，其中 N 为当前窗口的采样点数）
- 比单次 FFT 更稳定，适合短时、非平稳信号

**Step 2：瞬时振幅检测（Hilbert 变换）**

1. 对 0.5–2 Hz 带通滤波后的信号做 Hilbert 变换
2. 得到解析信号：$z(t) = x(t) + j\hat{x}(t)$
3. 瞬时振幅：$A(t) = |z(t)|$
4. 判定慢波：$A(t) \geq 75\,\mu\text{V}$

**Step 3：AASM 标准分类**

$$\text{isSWS} = \begin{cases} \text{True} & P_{\delta} > P_{th} \;\wedge\; R_{sw} \geq 0.20 \\ \text{False} & \text{otherwise} \end{cases}$$

其中 $R_{sw}$ 为慢波时间占比，$P_{th}$ 为 delta 功率数值阈值（默认 $10^{-6}$，为 Welch PSD 积分输出的经验下限，与具体硬件增益相关，可按被试调整）

**口述要点（90 秒）**
> 慢波检测分三步：第一步用 Welch 法计算 Delta 频段的功率，这比普通 FFT 对短时段信号更稳定；第二步用 Hilbert 变换求每个时间点的瞬时振幅，当振幅超过 75 微伏时判定为慢波；第三步综合 Delta 功率和慢波占比，按照 AASM 标准决定该 30 秒窗口是否为慢波睡眠。这两种方法结合——一个看频域，一个看时域——可以减少误判。

---

### 第 7 页：核心技术原理（四）— 特征提取

**PPT 要点**

**分窗策略**
- 每 30 秒一个时间窗（`epoch_size = 30.0 s`）
- 相邻窗口独立处理，不重叠

**提取的特征集**

| 特征名称 | 计算方式 | 含义 |
|---------|---------|------|
| `delta_power` | $\int_{0.5}^{4} \text{PSD}(f)df$ | Delta 频带能量 |
| `theta_power` | $\int_{4}^{8} \text{PSD}(f)df$ | Theta 频带能量 |
| `alpha_power` | $\int_{8}^{13} \text{PSD}(f)df$ | Alpha 频带能量 |
| `beta_power` | $\int_{13}^{30} \text{PSD}(f)df$ | Beta 频带能量 |
| `dominant_frequency` | $\arg\max_f \text{PSD}_\delta(f)$ | Delta 频段峰值频率 |
| `mean_amplitude` | $\text{mean}(|x(t)|)$ | 信号平均绝对幅度 |
| `avg_instantaneous_amplitude` | $\text{mean}(A(t))$ | 瞬时振幅均值（Hilbert） |
| `slow_wave_ratio` | $T_{sw} / T_{total}$ | 慢波时间占比 |

**特征平滑**
- 对时序特征做 5 窗口滑动均值（`smoothing_window=5`），抑制帧间抖动

**个体化统计（输入给映射层）**

```
delta_power_level : 'low' / 'medium' / 'high'  (基于 33/67 百分位)
amplitude_level   : 'low' / 'medium' / 'high'  (<50 / 50–100 / >100 μV)
frequency_level   : 'low' / 'medium' / 'high'  (<0.8 / 0.8–1.2 / >1.2 Hz)
```

**口述要点（60 秒）**
> 每 30 秒提取一次特征，包括四个频带的功率、主频率、平均振幅和瞬时振幅。提取后做滑动平均来抑制逐帧抖动。最重要的是最后一步：根据整段信号的统计分位数，把特征水平分为"低/中/高"三档，这三个档位直接决定后面音乐的音阶、速度和音色。

---

### 第 8 页：个体化音乐映射

**PPT 要点**

**映射设计原则**
1. **单调性**：特征增加，音乐参数方向固定
2. **有界性**：参数限制在可听范围内（tempo: 50–70 BPM）
3. **平滑性**：相邻窗口参数通过滑动平均过渡

**个体化配置表**（来自 `music/mapper.py`）

| EEG 特征 | 档位 | 音乐参数 | 具体值 |
|---------|-----|---------|-------|
| delta_power_level = low | 低慢波 | 音阶 | 五声音阶（Pentatonic） |
| delta_power_level = medium | 中等慢波 | 音阶 | 大调（Major） |
| delta_power_level = high | 高慢波 | 音阶 | 小调（Minor） |
| amplitude_level = low | 低振幅 | 音色 | Ambient Pad（MIDI 88） |
| amplitude_level = medium | 中振幅 | 音色 | Soft Piano（MIDI 0） |
| amplitude_level = high | 高振幅 | 音色 | Warm Strings（MIDI 49） |
| dominant_freq < 0.8 Hz | 极慢 | 节拍 | 50 BPM |
| dominant_freq 0.8–1.2 Hz | 中等 | 节拍 | 60 BPM |
| dominant_freq > 1.2 Hz | 较快 | 节拍 | 70 BPM |

**音符生成逻辑**

```
对每个 30 秒 epoch：
  1. 根据主频率计算基础音高：pitch = 60 + int((freq / 4.0) * 12)
  2. 量化到个体音阶（quantize_to_scale）
  3. 根据振幅映射力度：ratio = (amplitude - min_amp) / (max_amp - min_amp)
                        velocity = 40 + ratio * 40   # 力度范围 [40, 80]
  4. delta_power > 全局均值 * 0.8 → 生成三度和声音符
  5. delta_power > 全局均值 * 1.2 → 生成五度和声音符（增加丰富度）
```

**口述要点（90 秒）**
> 映射是整个系统最核心的创新点。我们没有使用统一规则，而是先根据被试自己的 EEG 统计分布，把 delta 功率、振幅和频率各分成三档，再据此选择最合适的音阶、音色和速度。这样不同被试会得到不同的个体化配置。在此基础上，每个时间窗的动态特征还会进一步控制具体音符的高低和力度，以及是否加入三度或五度和声，让音乐在宏观风格和微观细节上都与脑电状态对应。

---

### 第 9 页：音乐生成与输出

**PPT 要点**

**MIDI 生成流程**

```
音乐映射结果（音符列表）
  ↓
MIDIGenerator
  ├─ 设定 tempo（BPM）、音色（instrument program）
  ├─ 依次写入 Note On / Note Off 事件
  └─ 导出 .mid 文件

可选后处理：
  └─ AudioSynth → 合成 .wav / .mp3 音频（使用 pygame 或 FluidSynth）
```

**输出文件组织**（来自 `utils/config.py`）

```
output/
  └── subjects/{subject_id}/
        ├── midi/      # MIDI 文件
        ├── audio/     # 合成音频
        ├── csv/       # 特征导出
        └── data/      # 原始数据备份
```

**可展示的实验结果对比**

| 场景 | delta_power_level | 音阶 | Tempo | 音色 | 听感描述 |
|------|------------------|------|-------|------|---------|
| 浅睡 / 低慢波 | low | 五声音阶 | 50 BPM | Ambient Pad | 悠远、空旷 |
| 中等慢波 | medium | 大调 | 60 BPM | Soft Piano | 平静、舒缓 |
| 深度慢波睡眠 | high | 小调 | 70 BPM | Warm Strings | 厚重、沉稳 |

**口述要点（50 秒）**
> 最终输出是 MIDI 文件。MIDI 本质上是给电脑看的电子乐谱，记录了每个音符"何时响、响多高、响多响"，可以导入任何音乐软件加上音色播放。我们把结果按被试 ID 分目录存储，便于后续数据管理和实验复现。

---

### 第 10 页：当前研究进展

**PPT 要点**

**系统层面（工程闭环已打通）**
- ✅ 完成端到端可运行原型，覆盖"数据输入→预处理→检测→提取→映射→输出"全流程
- ✅ 支持多种数据格式（EDF/BDF/CSV/仿真信号），便于在无硬件条件下验证
- ✅ 实现 PyQt5 图形界面（`gui/` 模块），降低使用门槛
- ✅ 建立被试数据管理数据库（SQLite，`database/db_manager.py`）

**方法层面（可解释映射框架已建立）**
- ✅ 基于 AASM 标准的 SWS 自动检测（Welch PSD + Hilbert 瞬时振幅）
- ✅ 个体化参数配置策略（依据被试自身统计分位数，而非固定阈值）
- ✅ 特征平滑机制（5 窗口滑动均值），抑制帧间抖动

**验证层面（初步可行性已验证）**
- ✅ 仿真数据测试：不同 delta 功率水平可触发明显不同的音乐参数
- ✅ 系统可重复运行，结果可复现

**当前阶段定性结论**：  
> 技术路线"能跑通、有响应、可解释"，**可行性已验证，但稳定性和泛化性待进一步研究**。

---

### 第 11 页：当前存在的问题

**PPT 要点**（建议用"问题 → 根因 → 影响"三列格式展示）

**问题 1：数据输入依赖单通道，未充分利用多通道信息**
- 根因：当前特征提取只处理单一 EEG 通道（`get_channel_data(channel_idx=0)`）
- 影响：丢弃了脑区差异信息，空间分辨率为零

**问题 2：SWS 检测阈值固定，个体适应性不足**
- 根因：振幅阈值硬编码为 75 μV，delta 功率阈值为 $10^{-6}$
- 影响：不同被试、不同设备的信号幅度差异大，会造成漏检或误检

**问题 3：映射规则粒度粗，音乐表达能力有限**
- 根因：仅使用"低/中/高"三档离散化，动态映射分辨率低
- 影响：相邻睡眠状态生成的音乐相似度高，难以反映细微的脑状态变化

**问题 4：缺乏客观评价指标**
- 根因：目前无量化指标衡量"音乐是否准确反映了脑状态"
- 影响：不同算法版本之间无法严谨比较，结论说服力受限

**问题 5：音频合成依赖外部库，跨平台稳定性待验证**
- 根因：pygame / FluidSynth 的可用性因操作系统不同而存在差异
- 影响：在某些环境中只能输出 MIDI，无法直接试听音频

**问题 6：实时处理能力未验证**
- 根因：原型以离线批处理为主，未针对流式数据优化
- 影响：距离真实在线脑机交互场景仍有工程差距

---

### 第 12 页：下一步研究计划

**PPT 要点**（分三阶段）

#### 短期（当前阶段→可靠原型）

**目标：把原型做稳、做可比**

| 任务 | 具体措施 |
|------|---------|
| 多通道支持 | 扩展特征提取至多通道，支持通道选择或均值融合 |
| 自适应阈值 | 基于被试自身前 N 个 epoch 的统计量动态设定 SWS 阈值 |
| 基线对照实验 | 建立随机映射 / 固定模板两个对照组，验证 EEG 驱动的有效性 |
| 评价指标建立 | 参数稳定性方差、状态可分性（类间距离）、音乐连贯性评分 |

#### 中期（方法升级）

**目标：从规则驱动走向数据驱动+个体化**

| 任务 | 具体措施 |
|------|---------|
| 连续化映射 | 将三档离散映射升级为连续函数（线性/非线性插值） |
| 学习型映射 | 引入回归模型或轻量神经网络，从（EEG特征, 音乐偏好）对中学习映射关系 |
| 个体校准流程 | 设计短时校准协议（~5 min），生成被试专属的映射参数 |
| 音乐维度扩展 | 增加和声张力（和弦类型）、旋律走向、节奏密度等控制维度 |

#### 长期（平台化与应用验证）

**目标：形成可验证、可扩展的研究平台**

| 任务 | 具体措施 |
|------|---------|
| 实时流处理 | 实现基于滑动窗口的在线特征提取与音乐参数更新 |
| 主客观评价体系 | 主观：情绪一致性 / 自然度评分；客观：参数稳定性 / 时延 |
| 场景验证 | 睡眠辅助音乐生成、冥想引导、神经反馈训练 |
| 数据集与可复现性 | 整理实验数据集，发布标准化评测脚本 |

---

### 第 13 页：总结与讨论

**PPT 要点**

**核心贡献（当前阶段）**
1. 实现了从睡眠 EEG → 慢波检测 → 音乐映射 → MIDI 输出的完整闭环
2. 建立了基于 AASM 标准的 SWS 检测方法（Welch + Hilbert 双机制）
3. 提出了基于个体统计分位数的个体化音乐参数配置策略
4. 构建了可运行、可复现的 GUI 原型系统

**研究价值**
- 探索了"生理信号 → 音乐表达"的可解释映射框架
- 为脑机接口音乐应用提供了可扩展的工程基础

**一句话总结**
> 本阶段完成了技术路线验证。下一步将沿"稳定性 → 精细化 → 实时化"方向推进，把原型从"能跑通"发展为"能用、好用"的研究平台。

**讨论 / Q&A**

---

## 附录：常见评委提问与参考回答

### Q1：为什么不直接用端到端深度学习？
> 当前处于早期原型阶段，数据量有限（无大规模标注的脑电—音乐对），端到端深度学习需要大量训练数据。我们优先建立可解释的规则映射框架，为后续迁移到数据驱动方法提供对比基线和可解释性保障。

### Q2：你怎么证明生成的音乐不是随机的？
> 关键验证点：①相同 EEG 输入重复运行输出相同结果（可复现性）；②不同 delta 功率水平触发不同音阶/音色（已在仿真数据上观察）。下一步会与随机映射基线做统计对比来定量证明。

### Q3：为什么聚焦慢波睡眠，而不是一般认知状态的 EEG？
> 慢波睡眠的检测标准（AASM）相对明确，可以给特征提取和映射设计提供清晰的参照。这是一个边界清晰、可定量评估的切入点，有助于在早期原型阶段建立可靠的验证流程。

### Q4：75 μV 的振幅阈值是怎么确定的？
> 来自 AASM（美国睡眠医学会）慢波检测标准：高振幅慢波的典型阈值为 75 μV。这是领域通用参考值。后续会加入自适应阈值机制，针对不同设备和个体进行调整。

### Q5：个体化映射的效果如何评估？
> 目前主要依靠定性对比（不同状态对应不同音乐参数）。后续计划引入：①参数稳定性方差；②跨 epoch 状态可分性指标；③小规模用户主观评分（情绪一致性），建立主客观结合的评估框架。

---

## 汇报时间分配参考

| 内容 | 建议时间 |
|------|---------|
| 第 1–2 页（背景）| 2 分钟 |
| 第 3 页（架构）| 2 分钟 |
| 第 4–7 页（技术原理）| 5 分钟 |
| 第 8–9 页（映射与输出）| 3 分钟 |
| 第 10 页（进展）| 2 分钟 |
| 第 11–12 页（问题与计划）| 4 分钟 |
| 第 13 页（总结）| 1 分钟 |
| Q&A | 5–10 分钟 |

---

*本文档依据仓库实际代码（`eeg_processing/`、`music/`、`utils/config.py`）生成，所有技术参数均与代码实现保持一致。*
